export const myTodos = {
    state: {
       todos: []
    },

    reducers: {
       
    },

    effects: {
        // async asyncRemoveTodo(id) {
        //     await new Promise(resolve => setTimeout(() => resolve(), 1000))
        //     this.removeTodo(id)
        // }
    }
}